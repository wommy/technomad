extends(src='layout.sgr')
  block(name='content')
    h2 notice:
    li(mdi) had2 prepend `css & js` with `'/'` in layout.sgr
    li(mdi) post sub-template `extends` *works* yet borks terminal output
